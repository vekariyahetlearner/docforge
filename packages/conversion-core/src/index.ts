import { SHARED_PACKAGE_NAME } from '@docforge/shared';

/**
 * @docforge/conversion-core
 * Core conversion engine package boundary.
 * Framework-independent. Conversion abstractions will be introduced in Phase 01.
 */
export const CONVERSION_CORE_PACKAGE_NAME = '@docforge/conversion-core';
export const DEPENDS_ON = SHARED_PACKAGE_NAME;
